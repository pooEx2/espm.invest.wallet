spring.datasource.url=jdbc:mysql://127.0.0.1:3306/espm.invest.wallet
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=true

server.port=8082
spring.application.name=wallet-service
eureka.client.service-url.default-zone: ${EUREKA_URI:http://localhost:8761/eureka}
eureka.instance.prefer-ip-address=true

# -- TABELA DE TRANSACOES
# CREATE TABLE `espm.invest.wallet`.`exchangetransaction` (
#  `id_exchangetransaction` VARCHAR(64) NOT NULL,
#  `id_wallet` VARCHAR(64) NOT NULL,
#  `id_quotation` VARCHAR(64) NOT NULL,
#  `dt_date` DATE NOT NULL,
#  `nr_amount` DOUBLE NOT NULL,
#  `nr_type` INT NOT NULL,
#  PRIMARY KEY (`id_exchangetransaction`));

#CREATE TABLE `espm.invest.wallet`.`wallet` (
#  `id_wallet` VARCHAR(64) NOT NULL,
#  `id_user` VARCHAR(64) NULL,
#  `vl_balance` DOUBLE NOT NULL,
#  PRIMARY KEY (`id_wallet`));